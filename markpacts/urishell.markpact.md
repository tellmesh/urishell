# UriPack: urishell

Thin Markpact — capabilities from [`urishell/urishell/manifest.yaml`](urishell/urishell/manifest.yaml).
Handlers: existing ``python://`` refs (code in `urishell/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: urishell
  version: '1'
  language: python
description: Subprocess shell commands on the automation host.
schemes:
- shell
capabilities:
- id: shell-run
  uri: shell://{command}
  kind: command
  operation: shell.run
  handler: python://urishell.handlers:shell_run
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: shell
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port: 8789
  path: /uri/call
flow:
  ids:
  - shell-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: shell.run_command
  uri: shell://echo
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: shell.run
  payload: {}
```

```yaml markpact:flow id=shell-smoke
flow:
  id: shell-smoke
  description: Smoke shell:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- shell://echo: {}
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd urishell
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/urishell.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8789
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `urishell/urishell/manifest.yaml`
- Requires: `pip install -e urishell` or `TELLMESH_ROOT` for python:// handlers
```
